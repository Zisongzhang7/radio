# EC11 旋转编码器快速参考

## 📌 引脚连接

| EC11 | ESP32 | 说明 |
|------|-------|------|
| CLK  | GPIO 10 | 时钟信号 (A相) |
| DT   | GPIO 9  | 数据信号 (B相) |
| SW   | GPIO 8  | 按键开关 |
| +    | 3.3V    | 电源 |
| GND  | GND     | 接地 |

## 🚀 快速启动

### 1. 编译烧录
```bash
cd /Users/zhangzisong/Desktop/test1.1/xiaozhi-esp32-main
idf.py build flash monitor
```

### 2. 测试功能
- **旋转编码器** → 查看监控台输出旋转方向
- **按下按钮** → 查看监控台输出按键事件

## 📊 监控输出示例

```
I (1234) main: EC11旋转编码器已启动 (CLK=GPIO10, DT=GPIO9, SW=GPIO8)
I (5678) RotaryEncoder: 顺时针旋转 [计数: 1]
I (5679) main: ==> 旋转编码器: 顺时针旋转
I (7890) RotaryEncoder: 逆时针旋转 [计数: 0]
I (7891) main: ==> 旋转编码器: 逆时针旋转
I (8901) RotaryEncoder: 按钮按下
I (8902) main: ==> 旋转编码器: 按钮按下
I (9012) RotaryEncoder: 按钮释放
I (9013) main: ==> 旋转编码器: 按钮释放
```

## 🔧 文件结构

```
main/
├── input/
│   ├── rotary_encoder.h      # 头文件
│   └── rotary_encoder.cc     # 实现文件
├── main.cc                    # 已集成编码器
└── CMakeLists.txt             # 已更新配置
```

## 💡 事件类型

| 事件 | 说明 |
|------|------|
| `ROTATE_CW` | 顺时针旋转 |
| `ROTATE_CCW` | 逆时针旋转 |
| `BUTTON_PRESS` | 按钮按下 |
| `BUTTON_RELEASE` | 按钮释放 |

## ⚙️ 主要 API

```cpp
// 初始化编码器
RotaryEncoder encoder(GPIO_NUM_10, GPIO_NUM_9, GPIO_NUM_8);
encoder.Initialize();

// 设置事件回调
encoder.SetEventCallback(callback_function, user_data);

// 启动监听
encoder.Start();

// 获取旋转计数
int32_t count = encoder.GetRotationCount();

// 重置计数
encoder.ResetRotationCount();

// 检查按钮状态
bool pressed = encoder.IsButtonPressed();

// 停止监听
encoder.Stop();
```

## 🎯 应用示例

### 音量控制
```cpp
case ROTATE_CW:   volume++;      // 增加音量
case ROTATE_CCW:  volume--;      // 减少音量
case BUTTON_PRESS: mute = !mute; // 静音切换
```

### 菜单导航
```cpp
case ROTATE_CW:    menu_index++; // 下一项
case ROTATE_CCW:   menu_index--; // 上一项
case BUTTON_PRESS: select();     // 选择
```

## ⚠️ 故障排查

| 问题 | 解决方案 |
|------|----------|
| 无响应 | 检查接线和供电 |
| 方向相反 | 交换CLK和DT引脚 |
| 按键无效 | 检查SW引脚连接 |
| GPIO冲突 | 确认GPIO 8/9/10未被占用 |

## 📚 完整文档

详细信息请查看：`EC11_旋转编码器使用说明.md`

---

✅ **集成完成** - 旋转编码器已完全集成到项目中并可直接使用！

