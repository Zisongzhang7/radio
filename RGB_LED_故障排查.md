# RGB LED 故障排查指南

## 🔌 硬件连接检查清单

### WS2812 LED 标准接线（必须全部连接）

| LED 引脚 | 连接到 ESP32 | 说明 |
|---------|-------------|------|
| VCC / +5V | 5V 引脚 | 电源正极 ✅ |
| GND / - | GND 引脚 | 接地线 ⚠️ **必须连接** |
| DIN / DATA | GPIO 14 | 数据信号线 ⚠️ **必须连接** |

### 常见 LED 引脚标识

不同的 WS2812 模块可能有不同的标识：

```
标准模块：     VCC   GND   DIN
三线模块：     +5V    -    DATA
灯带模块：     5V    GND   D0/DI
```

## ❌ 常见错误

### 错误 1：只接了 VCC 和信号线，忘记接 GND

```
❌ 错误接线：
ESP32                   LED
5V      ------------->  VCC
GPIO14  ------------->  DIN
                        GND (悬空) ← 错误！

✅ 正确接线：
ESP32                   LED
5V      ------------->  VCC
GND     ------------->  GND  ← 必须连接！
GPIO14  ------------->  DIN
```

### 错误 2：接线顺序混乱

- 请仔细核对 LED 模块上的丝印标识
- 不要把 VCC 和 GND 接反（会烧坏 LED）

### 错误 3：数据方向接反

- WS2812 灯带有方向：**DIN (输入)** 和 **DOUT (输出)**
- 信号必须从 **DIN** 端输入
- 检查灯带上的箭头标识 →

## 🔧 解决方案

### 方案 1：检查基础连接（最可能）

1. **断电**，检查三根线是否都正确连接
2. 用万用表测试 LED 的 VCC 和 GND 之间是否有 5V 电压
3. 确认 GPIO14 已连接到 LED 的 DIN 引脚
4. **重新上电**，观察 LED 是否亮起

### 方案 2：添加保护电阻

在信号线上串联一个电阻可以提高稳定性：

```
ESP32 GPIO14 ---[330Ω]--- LED DIN
```

这个电阻可以：
- 保护 GPIO 引脚
- 改善信号质量
- 减少电磁干扰

### 方案 3：缩短连接线

如果使用的杜邦线太长（超过 20cm），信号可能会衰减：

- 尽量使用 **短线**（10cm 以内最佳）
- 避免信号线和电源线缠绕在一起

### 方案 4：增加电容（可选）

如果供电不稳定，可以在 LED 的 VCC 和 GND 之间并联一个 **100μF 电容**，改善供电质量。

### 方案 5：电平转换（少见情况）

极少数 WS2812 可能需要 5V 逻辑信号：

```
ESP32 GPIO14 → 电平转换器 (3.3V→5V) → LED DIN
```

但大多数 WS2812 可以接受 3.3V 信号，通常不需要这一步。

## 📝 查看日志

连接串口，查看是否有以下日志输出：

```bash
# 连接串口监视器
idf.py -p /dev/ttyUSB0 monitor

# 或在 macOS 上
idf.py -p /dev/cu.usbserial-* monitor
```

### 正常日志输出应该包含：

```
I (xxxx) RandomRgbLed: 初始化 RGB LED，GPIO: 14, 切换间隔: 1000 ms
I (xxxx) RandomRgbLed: RGB LED 初始化完成
I (xxxx) RandomRgbLed: 启动随机颜色切换
I (xxxx) main: RGB LED 已启动，将自动随机切换颜色
I (xxxx) RandomRgbLed: 切换颜色: 红色 (R:20 G:0 B:0)
I (xxxx) RandomRgbLed: 切换颜色: 蓝色 (R:0 G:0 B:20)
...
```

### 如果看到错误日志：

```
E (xxxx) RandomRgbLed: 创建 LED strip 失败: ...
```

说明硬件初始化失败，请检查：
1. GPIO14 是否被其他功能占用
2. RMT 外设是否可用
3. 板子型号是否支持

## 🧪 测试步骤

### 步骤 1：基础连接测试

1. 断开所有连接
2. 重新连接三根线：
   - LED VCC → 板子 5V
   - LED GND → 板子 GND
   - LED DIN → 板子 GPIO14
3. 上电，等待 5 秒
4. 观察 LED 是否开始变色

### 步骤 2：LED 本身测试

如果 LED 本身有问题，可以用简单的方法测试：

```cpp
// 临时测试代码：让 LED 保持红色
led_strip_set_pixel(led_strip_, 0, 50, 0, 0);  // 红色，亮度 50
led_strip_refresh(led_strip_);
```

如果还是不亮，LED 模块本身可能损坏。

### 步骤 3：GPIO 测试

临时修改 GPIO 引脚进行测试：

在 `main/main.cc` 中：
```cpp
// 尝试不同的 GPIO 引脚
#define RGB_LED_GPIO GPIO_NUM_13  // 改用 GPIO13 测试
// 或
#define RGB_LED_GPIO GPIO_NUM_15  // 改用 GPIO15 测试
```

**注意**：确保选择的 GPIO 没有被其他功能占用。

## 🔍 硬件替代方案

如果确认硬件连接正确但仍不亮，可能的原因：

1. **LED 模块损坏** - 更换新的 WS2812 模块
2. **GPIO 冲突** - 该 GPIO 可能被板子的其他功能占用
3. **电源不足** - 如果使用 USB 供电，电流可能不够

### 更换 GPIO 引脚

如果 GPIO14 被占用，可以使用其他可用 GPIO：

**推荐的备选 GPIO**（ESP32-S3）:
- GPIO 15
- GPIO 16  
- GPIO 21
- GPIO 47
- GPIO 48

修改方法：在 `main/main.cc` 中：
```cpp
#define RGB_LED_GPIO GPIO_NUM_15  // 改用 GPIO15
```

## 📞 进一步排查

如果以上方法都不行，请提供：

1. **硬件照片**：LED 模块和连接线的照片
2. **串口日志**：完整的启动日志
3. **LED 型号**：LED 模块的具体型号（如果知道）
4. **板子型号**：ESP32 开发板的具体型号

这样我可以提供更精确的解决方案！

## ⚡ 快速自检

```
□ LED VCC 已连接到 5V
□ LED GND 已连接到 GND
□ LED DIN 已连接到 GPIO14
□ LED 方向正确（如果是灯带）
□ 连接线没有断开或接触不良
□ 程序已成功编译并烧录到板子
□ 串口日志显示 "RGB LED 已启动"
```

如果以上全部打勾 ✅，LED 应该会工作。如果还是不亮，很可能是 LED 模块本身有问题。

