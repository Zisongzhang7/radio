# 按钮聆听功能使用说明

## 📌 功能概述

旋转编码器按钮现在支持点击切换聆听状态，并在聆听时将所有 RGB LED 显示为绿色。

## 🎮 使用方法

### 基本操作

1. **进入聆听状态**
   - 按下旋转编码器按钮
   - 松开按钮
   - ✅ 设备进入聆听状态
   - 🟢 RGB LED 变为绿色

2. **退出聆听状态**
   - 再次按下并松开按钮
   - ✅ 设备退出聆听状态
   - 🎨 RGB LED 恢复随机颜色切换

3. **音量调节**（旋转编码器）
   - 🔄 顺时针旋转：音量增加
   - 🔄 逆时针旋转：音量减少

## 🔧 技术实现

### 修改的文件

1. **`main/led/random_rgb_led.h`**
   - 新增 `SetColor(r, g, b)` 方法
   - 新增 `Resume()` 方法
   - 新增固定颜色模式标志

2. **`main/led/random_rgb_led.cc`**
   - 实现固定颜色设置功能
   - 实现恢复随机颜色功能
   - 修改定时器回调逻辑

3. **`main/main.cc`**
   - 使用 `ToggleChatState()` 切换聆听状态
   - 注册设备状态变化回调
   - 在聆听状态下设置 RGB LED 为绿色
   - 退出聆听状态时恢复随机颜色

### 工作流程

```
用户操作: 按下按钮 → 松开按钮
         ↓
应用逻辑: ToggleChatState()
         ↓
状态切换: kDeviceStateIdle → kDeviceStateListening
         ↓
事件触发: DeviceStateEventManager 发送状态变化事件
         ↓
回调处理: 检测到进入聆听状态
         ↓
LED控制: rgb_led.SetColor(0, 100, 0) // 绿色
```

### 状态转换说明

| 当前状态 | 按钮操作 | 新状态 | RGB LED 颜色 |
|---------|---------|--------|-------------|
| 待命 (Idle) | 点击按钮 | 聆听 (Listening) | 🟢 绿色 |
| 聆听 (Listening) | 点击按钮 | 待命 (Idle) | 🎨 随机颜色 |
| 说话 (Speaking) | 点击按钮 | 中止说话 | 根据新状态决定 |

## 🌈 RGB LED 颜色说明

### 固定颜色（聆听状态）
- **绿色**：RGB(0, 100, 0)
  - 表示设备正在聆听
  - 可以进行语音交互

### 随机颜色（其他状态）
每秒随机切换以下颜色：
1. 🔴 红色 (100, 0, 0)
2. 🟢 绿色 (0, 100, 0)
3. 🔵 蓝色 (0, 0, 100)
4. 🟡 黄色 (100, 100, 0)
5. 🟣 紫色 (100, 0, 100)
6. 🔷 青色 (0, 100, 100)
7. 🟠 橙色 (100, 50, 0)
8. 🩷 粉色 (100, 0, 50)

## 🔌 硬件连接

| 组件 | GPIO 引脚 | 说明 |
|-----|----------|------|
| RGB LED 信号 | GPIO 14 | WS2812 LED 控制 |
| 编码器 CLK (A相) | GPIO 10 | 旋转检测 |
| 编码器 DT (B相) | GPIO 9 | 旋转方向 |
| 编码器 SW (按钮) | GPIO 8 | 点击控制 |

## 📝 日志输出示例

```
I (12345) main: ==> 旋转编码器: 按钮按下
I (12450) main: ==> 旋转编码器: 按钮释放 - 切换聆听状态
I (12451) main: 进入聆听状态，RGB LED 显示绿色
I (12452) RandomRgbLed: 🎨 设置固定颜色 (R:0 G:100 B:0)
I (12453) Application: STATE: Listening

... 用户说话并得到回复 ...

I (15678) main: ==> 旋转编码器: 按钮释放 - 切换聆听状态
I (15679) main: 退出聆听状态，RGB LED 恢复随机颜色
I (15680) RandomRgbLed: 🎨 恢复随机颜色切换
I (15681) RandomRgbLed: 🎨 切换颜色: 蓝色 (R:0 G:0 B:100)
I (15682) Application: STATE: Idle
```

## 💡 使用提示

1. **点击操作**：按下后立即松开，不需要长按
2. **状态切换**：可以在待命和聆听状态之间随意切换
3. **自动停止**：根据 AEC 配置，聆听可能会自动停止
4. **音量调节**：在任何状态下都可以旋转编码器调节音量

## 🎯 设计目标

✅ 点击式交互，而非按住通话
✅ 清晰的视觉反馈（绿色 = 聆听）
✅ 状态可切换（点击进入/退出）
✅ 保持原有的音量调节功能

## 🔄 与原有功能的对比

| 功能 | 旧版本 | 新版本 |
|-----|--------|--------|
| 进入聆听 | 按住按钮 | 点击按钮 |
| 聆听状态 | 按住期间 | 持续到再次点击或自动结束 |
| RGB 反馈 | 随机颜色 | 聆听时显示绿色 |
| 音量调节 | ✅ 支持 | ✅ 保持不变 |

## 🚀 编译和使用

代码已经修改完成，可以直接编译和烧录：

```bash
# 如果有 ESP-IDF 环境
idf.py build
idf.py flash monitor

# 或者使用项目提供的脚本
./build_with_alarm.sh
```

烧录后即可使用新的按钮聆听功能！









