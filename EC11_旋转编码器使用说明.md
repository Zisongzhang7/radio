# EC11 旋转编码器使用说明

## 概述

本项目已集成 EC11 旋转编码器支持，可以检测旋转方向和按键事件。

## 硬件连接

### EC11 编码器引脚定义

| EC11 引脚 | 功能 | ESP32 GPIO | 说明 |
|-----------|------|------------|------|
| CLK (A相) | 时钟信号 | GPIO 10 | 旋转信号A |
| DT (B相)  | 数据信号 | GPIO 9  | 旋转信号B |
| SW        | 按键开关 | GPIO 8  | 按下为低电平 |
| +         | 电源正极 | 3.3V    | 供电 |
| GND       | 接地     | GND     | 接地 |

### 接线方式

```
EC11 编码器          ESP32 开发板
───────────────     ────────────────
CLK (A)     ────────> GPIO 10
DT  (B)     ────────> GPIO 9
SW          ────────> GPIO 8
+           ────────> 3.3V
GND         ────────> GND
```

## 功能特性

### 1. 旋转检测
- **顺时针旋转**: 编码器顺时针转动时触发
- **逆时针旋转**: 编码器逆时针转动时触发
- **旋转计数**: 自动记录旋转次数（正数=顺时针，负数=逆时针）

### 2. 按键检测
- **按键按下**: 按下编码器按钮时触发
- **按键释放**: 释放编码器按钮时触发
- **按键消抖**: 自动50ms消抖处理

### 3. 监控输出
所有事件都会输出到串口监控台，方便调试和测试。

## 监控输出示例

编译并烧录程序后，打开串口监控器（波特率 115200），你将看到类似以下输出：

```
I (1234) main: EC11旋转编码器已启动 (CLK=GPIO10, DT=GPIO9, SW=GPIO8)
I (1235) main: 测试说明: 旋转编码器将输出旋转方向和按键事件到监控台
I (1236) RotaryEncoder: 监听任务开始运行

// 当你顺时针旋转编码器时：
I (5678) RotaryEncoder: 顺时针旋转 [计数: 1]
I (5679) main: ==> 旋转编码器: 顺时针旋转

I (6789) RotaryEncoder: 顺时针旋转 [计数: 2]
I (6790) main: ==> 旋转编码器: 顺时针旋转

// 当你逆时针旋转编码器时：
I (7890) RotaryEncoder: 逆时针旋转 [计数: 1]
I (7891) main: ==> 旋转编码器: 逆时针旋转

// 当你按下按钮时：
I (8901) RotaryEncoder: 按钮按下
I (8902) main: ==> 旋转编码器: 按钮按下

// 当你释放按钮时：
I (9012) RotaryEncoder: 按钮释放
I (9013) main: ==> 旋转编码器: 按钮释放
```

## 编译和烧录

### 1. 编译项目

```bash
cd /Users/zhangzisong/Desktop/test1.1/xiaozhi-esp32-main
idf.py build
```

### 2. 烧录固件

```bash
idf.py -p /dev/tty.usbserial-* flash
```

> 注意：替换 `/dev/tty.usbserial-*` 为你的实际串口设备名称

### 3. 查看监控输出

```bash
idf.py -p /dev/tty.usbserial-* monitor
```

或者一步完成烧录和监控：

```bash
idf.py -p /dev/tty.usbserial-* flash monitor
```

## 测试步骤

1. **连接硬件**: 按照上述接线方式连接 EC11 编码器到 ESP32
2. **编译烧录**: 使用上述命令编译并烧录固件
3. **打开监控**: 打开串口监控查看输出
4. **测试旋转**: 
   - 顺时针旋转编码器，观察监控输出
   - 逆时针旋转编码器，观察监控输出
5. **测试按键**:
   - 按下编码器按钮，观察监控输出
   - 释放按钮，观察监控输出

## 代码说明

### 文件结构

```
main/
├── input/
│   ├── rotary_encoder.h      # 旋转编码器头文件
│   └── rotary_encoder.cc     # 旋转编码器实现
└── main.cc                    # 主程序（已集成编码器）
```

### 核心类：RotaryEncoder

#### 构造函数
```cpp
RotaryEncoder(gpio_num_t clk_gpio, gpio_num_t dt_gpio, gpio_num_t sw_gpio);
```

#### 主要方法
- `bool Initialize()`: 初始化编码器
- `void Start()`: 启动监听任务
- `void Stop()`: 停止监听任务
- `void SetEventCallback(callback, user_data)`: 设置事件回调
- `int32_t GetRotationCount()`: 获取旋转计数
- `void ResetRotationCount()`: 重置旋转计数
- `bool IsButtonPressed()`: 获取按钮状态

#### 事件类型
```cpp
enum EventType {
    ROTATE_CW,      // 顺时针旋转
    ROTATE_CCW,     // 逆时针旋转
    BUTTON_PRESS,   // 按钮按下
    BUTTON_RELEASE  // 按钮释放
};
```

## 自定义事件处理

你可以在 `main.cc` 中修改 `RotaryEncoderEventHandler` 函数来实现自定义功能：

```cpp
void RotaryEncoderEventHandler(RotaryEncoder::EventType event, void* user_data)
{
    switch (event) {
        case RotaryEncoder::ROTATE_CW:
            // 添加你的顺时针旋转处理代码
            break;
        case RotaryEncoder::ROTATE_CCW:
            // 添加你的逆时针旋转处理代码
            break;
        case RotaryEncoder::BUTTON_PRESS:
            // 添加你的按钮按下处理代码
            break;
        case RotaryEncoder::BUTTON_RELEASE:
            // 添加你的按钮释放处理代码
            break;
    }
}
```

## 故障排查

### 1. 编码器无响应
- 检查接线是否正确
- 检查供电是否正常（3.3V）
- 检查 GPIO 引脚是否与其他功能冲突

### 2. 旋转方向相反
- 交换 CLK 和 DT 引脚连接

### 3. 按键无响应
- 检查 SW 引脚连接
- 确认按键是否正常工作

### 4. 编译错误
- 确保所有文件都已正确添加
- 检查 CMakeLists.txt 是否正确配置

## 技术参数

- **工作电压**: 3.3V
- **脉冲数**: 20 脉冲/圈
- **旋转角度**: 360° 连续旋转
- **按键寿命**: > 50,000 次
- **消抖延迟**: 50ms（按键）/ 5ms（旋转）

## 注意事项

1. **电压兼容性**: EC11 编码器通常支持 3.3V 和 5V，本项目使用 3.3V
2. **引脚冲突**: 确保 GPIO 8、9、10 没有被其他功能占用
3. **中断优先级**: 编码器使用 GPIO 中断，优先级设置为 5
4. **任务堆栈**: 监听任务使用 4096 字节堆栈空间

## 应用示例

### 音量控制
```cpp
case RotaryEncoder::ROTATE_CW:
    volume += 1;  // 增加音量
    break;
case RotaryEncoder::ROTATE_CCW:
    volume -= 1;  // 减少音量
    break;
case RotaryEncoder::BUTTON_PRESS:
    mute = !mute; // 切换静音
    break;
```

### 菜单导航
```cpp
case RotaryEncoder::ROTATE_CW:
    menu_index++;  // 下一个菜单项
    break;
case RotaryEncoder::ROTATE_CCW:
    menu_index--;  // 上一个菜单项
    break;
case RotaryEncoder::BUTTON_PRESS:
    select_menu_item(menu_index);  // 选择当前项
    break;
```

## 更多信息

如有问题或建议，请查看项目文档或提交 Issue。

