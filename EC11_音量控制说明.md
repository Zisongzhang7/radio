# EC11 旋转编码器音量控制说明

## 功能概述

已成功实现 EC11 旋转编码器的音量控制功能：
- **顺时针旋转**：增加音量（每次 +5，快速调节）
- **逆时针旋转**：减少音量（每次 -5，快速调节）
- **音量范围**：0 - 100
- **自动限制**：音量会自动限制在有效范围内
- **声音提示**：每次调节音量时会播放简短的提示音效

## 硬件连接

| EC11 引脚 | ESP32 GPIO | 功能说明 |
|-----------|------------|----------|
| CLK (A相) | GPIO 10 | 时钟信号 |
| DT (B相)  | GPIO 9  | 数据信号 |
| SW        | GPIO 8  | 按键开关 |
| +         | 3.3V    | 电源正极 |
| GND       | GND     | 接地 |

```
EC11 编码器          ESP32 开发板
───────────────     ────────────────
CLK (A)     ────────> GPIO 10
DT  (B)     ────────> GPIO 9
SW          ────────> GPIO 8
+           ────────> 3.3V
GND         ────────> GND
```

## 使用方法

### 1. 调整音量

#### 增加音量
- 顺时针旋转编码器
- 每次旋转音量增加 5（快速调节）
- 最高到达 100 后不再增加
- 每次调节播放"嘟"的提示音

#### 减少音量
- 逆时针旋转编码器
- 每次旋转音量减少 5（快速调节）
- 最低到达 0 后不再减少
- 每次调节播放"嘟"的提示音

### 2. 查看音量变化

编译并烧录程序后，打开串口监控器（波特率 115200），旋转编码器时会看到：

```
I (5678) main: ==> 旋转编码器: 顺时针旋转 - 音量: 50 -> 55
I (6789) main: ==> 旋转编码器: 顺时针旋转 - 音量: 55 -> 60
I (7890) main: ==> 旋转编码器: 逆时针旋转 - 音量: 60 -> 55
I (8901) main: ==> 旋转编码器: 逆时针旋转 - 音量: 55 -> 50
```

### 3. 声音提示说明

每次调节音量时，系统会播放一个简短的 **"嘟"** 提示音（popup 音效）：
- 提示音非常简短，不会影响正常使用
- 提供即时的音量调节反馈
- 可以通过声音判断旋转编码器是否工作正常

### 3. 音量范围限制示例

**到达最大值：**
```
I (1234) main: ==> 旋转编码器: 顺时针旋转 - 音量: 95 -> 100
I (1235) main: ==> 旋转编码器: 顺时针旋转 - 音量: 100 -> 100  (已达上限)
```

**到达最小值：**
```
I (2345) main: ==> 旋转编码器: 逆时针旋转 - 音量: 5 -> 0
I (2346) main: ==> 旋转编码器: 逆时针旋转 - 音量: 0 -> 0  (已达下限)
```

## 编译和烧录

### 1. 编译项目

```bash
cd /Users/zhangzisong/Desktop/test1.1/xiaozhi-esp32-main
idf.py build
```

### 2. 烧录固件

```bash
idf.py -p /dev/tty.usbserial-* flash
```

> 注意：替换 `/dev/tty.usbserial-*` 为你的实际串口设备名称

### 3. 查看监控输出

```bash
idf.py -p /dev/tty.usbserial-* monitor
```

或者一步完成烧录和监控：

```bash
idf.py -p /dev/tty.usbserial-* flash monitor
```

## 代码实现说明

### 修改的文件

- `main/main.cc` - 添加了音量控制逻辑和声音提示功能

### 关键实现

#### 1. 音量提示音播放函数

```cpp
void PlayVolumeSound(int volume) {
    static int64_t last_play_time = 0;
    int64_t current_time = esp_timer_get_time() / 1000; // 毫秒
    
    // 防抖：只有距离上次播放超过300ms才播放新的提示音
    if (current_time - last_play_time >= 300) {
        auto& app = Application::GetInstance();
        app.PlaySound(Lang::Sounds::OGG_POPUP);
        last_play_time = current_time;
    }
}
```

**防抖机制说明**：
- 快速旋转时，自动限制提示音播放频率
- 避免声音堆积，提升使用体验
- 默认300ms防抖间隔，可自定义调整

#### 2. 旋转编码器事件处理

```cpp
void RotaryEncoderEventHandler(RotaryEncoder::EventType event, void* user_data)
{
    auto& board = Board::GetInstance();
    auto codec = board.GetAudioCodec();
    
    switch (event) {
        case RotaryEncoder::ROTATE_CW:
            // 顺时针旋转 - 增加音量
            int current_volume = codec->output_volume();
            int new_volume = current_volume + 5;  // 每次+5，快速调节
            if (new_volume > 100) new_volume = 100;
            codec->SetOutputVolume(new_volume);
            PlayVolumeSound(new_volume);  // 播放音量提示
            break;
            
        case RotaryEncoder::ROTATE_CCW:
            // 逆时针旋转 - 减少音量
            int current_volume = codec->output_volume();
            int new_volume = current_volume - 5;  // 每次-5，快速调节
            if (new_volume < 0) new_volume = 0;
            codec->SetOutputVolume(new_volume);
            PlayVolumeSound(new_volume);  // 播放音量提示
            break;
    }
}
```

## 自定义设置

### 修改音量调整步长

如果想修改每次旋转的音量变化量，可以编辑 `main/main.cc` 文件：

```cpp
// 修改增加步长（默认为5）
int new_volume = current_volume + 5;  // 改为你想要的值，如 1, 2, 10 等

// 修改减少步长（默认为5）
int new_volume = current_volume - 5;  // 改为你想要的值，如 1, 2, 10 等
```

**推荐步长值：**
- `1` - 精确调节（最精细控制）
- `2` - 适中调节（平衡性能）
- `5` - 快速调节（默认值，快速变化）
- `10` - 超快调节（大幅变化）

### 修改音量范围

如果需要修改音量范围（默认 0-100），可以修改限制条件：

```cpp
// 修改最大音量（默认100）
if (new_volume > 100) {
    new_volume = 100;  // 改为你想要的最大值
}

// 修改最小音量（默认0）
if (new_volume < 0) {
    new_volume = 0;    // 改为你想要的最小值
}
```

### 更换提示音

如果想使用不同的提示音，可以修改 `PlayVolumeSound` 函数：

```cpp
void PlayVolumeSound(int volume) {
    auto& app = Application::GetInstance();
    
    // 可以使用其他音效替换 OGG_POPUP
    app.PlaySound(Lang::Sounds::OGG_SUCCESS);    // 成功音
    // app.PlaySound(Lang::Sounds::OGG_VIBRATION); // 震动音
    // app.PlaySound(Lang::Sounds::OGG_EXCLAMATION); // 感叹音
}
```

### 禁用声音提示

如果不想要声音提示，只保留音量调节功能，可以注释掉 `PlayVolumeSound` 调用：

```cpp
case RotaryEncoder::ROTATE_CW:
    // ...
    codec->SetOutputVolume(new_volume);
    // PlayVolumeSound(new_volume);  // 注释掉这一行
    break;
```

### 添加音量数字播报

如果想在调节时播报具体数字，可以参考以下实现（需要注意会增加响应时间）：

```cpp
void PlayVolumeSound(int volume) {
    auto& app = Application::GetInstance();
    
    // 播放提示音
    app.PlaySound(Lang::Sounds::OGG_POPUP);
    vTaskDelay(pdMS_TO_TICKS(200));
    
    // 播放音量数字
    if (volume >= 10) {
        int tens = volume / 10;
        // 播放十位数...
    }
    // 播放个位数...
}
```

> 注意：由于当前默认步长为1，频繁播报数字可能会很嘈杂，建议配合较大步长使用（如5或10）

## 测试步骤

1. **连接硬件**：按照接线图连接 EC11 编码器到 ESP32
2. **编译烧录**：执行 `idf.py build flash monitor`
3. **测试增加音量**：
   - 顺时针旋转编码器
   - 观察串口输出的音量变化
   - 持续旋转直到达到 100
4. **测试减少音量**：
   - 逆时针旋转编码器
   - 观察串口输出的音量变化
   - 持续旋转直到达到 0
5. **测试音频输出**：
   - 播放音频时旋转编码器
   - 验证音量实际发生变化

## 故障排查

### 旋转编码器无响应
- 检查接线是否正确（CLK=GPIO10, DT=GPIO9, SW=GPIO8）
- 检查供电是否正常（3.3V）
- 查看串口是否有初始化成功的日志

### 音量不变化
- 确认音频编解码器已正确初始化
- 检查是否有其他代码覆盖了音量设置
- 查看串口日志确认音量值确实在改变

### 旋转方向相反
- 如果顺时针减少、逆时针增加，交换 CLK 和 DT 引脚连接
- 或在代码中交换 ROTATE_CW 和 ROTATE_CCW 的处理逻辑

### 音量跳跃过快
- 减小音量调整步长（从 5 改为 1 或 2）
- 检查编码器是否正常工作（可能有接触不良）

### 提示音太频繁或堆积
- ✅ **已优化**：添加了防抖机制，限制播放频率为每300ms最多一次
- 快速旋转时自动跳过部分提示音，避免声音堆积
- 如需调整：修改 `PlayVolumeSound()` 函数中的防抖时间（默认300ms）
- 如果完全不需要提示音，可以注释掉 `PlayVolumeSound(new_volume);`
- 详细说明请查看：[EC11_音量控制_防抖优化说明.md](EC11_音量控制_防抖优化说明.md)

### 没有提示音
- 检查音频编解码器是否正常工作
- 确认系统音量不是0
- 查看串口日志确认音量确实在改变

## 技术参数

- **工作电压**：3.3V
- **音量范围**：0 - 100
- **调整步长**：5（快速调节，可自定义）
- **响应延迟**：< 50ms
- **消抖时间**：5ms（旋转）/ 50ms（按键）
- **提示音效**：popup（简短的"嘟"声）

## 未来扩展

### 按键功能
当前按键事件已检测但未实现功能，可以添加：
- 按下切换静音
- 长按重置音量到默认值
- 双击切换音频输出设备

示例代码：
```cpp
case RotaryEncoder::BUTTON_PRESS:
    // 切换静音
    static bool muted = false;
    muted = !muted;
    if (muted) {
        codec->SetOutputVolume(0);
        ESP_LOGI(TAG, "音量已静音");
    } else {
        codec->SetOutputVolume(50);  // 恢复到默认音量
        ESP_LOGI(TAG, "音量已恢复");
    }
    break;
```

## 相关文档

- [EC11_旋转编码器使用说明.md](EC11_旋转编码器使用说明.md) - 完整的 EC11 使用文档
- [EC11_快速参考.md](EC11_快速参考.md) - EC11 快速参考指南

---

✅ **音量控制功能已完成并可直接使用！**

如有问题或需要帮助，请查看项目文档或提交 Issue。

